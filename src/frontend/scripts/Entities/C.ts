/**
 * ...
 */
export namespace C {
    /**
     * ...
     */
    export enum visibility {
        VISIBLE = 'visible',
        SHOWING = 'showing',
        HIDDEN = 'hidden',
        HIDING = 'hiding',
    }

    /**
     * ...
     */
    export enum side {
        LEFT = 'left',
        RIGHT = 'right',
    }

    /**
     * ...
     */
    export enum action {
        SHOW = 'show',
        HIDE = 'hide',
        BACK = 'back',
    }

    /**
     * ...
     */
    export enum location {
        INSIDE = 'inside',
        OUTSIDE = 'outside',
        EVERYWHERE = 'everywhere',
    }
}

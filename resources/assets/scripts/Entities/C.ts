/**
 * ...
 */
export namespace C {
    /**
     * ...
     */
    export enum visibility {
        VISIBLE = 'visible',
        HIDDEN = 'hidden',
    }

    /**
     * ...
     */
    export enum side {
        LEFT = 'left',
        RIGHT = 'right',
    }

    /**
     * ...
     */
    export enum action {
        SHOW = 'show',
        HIDE = 'hide',
    }

    /**
     * ...
     */
    export enum location {
        INTERNAL = 'internal',
        EXTERNAL = 'external',
        UBIQUITOUS = 'ubiquitous',
    }
}
